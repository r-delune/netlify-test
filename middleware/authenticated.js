export default function ({ store, redirect }) {
    // If the user is not authenticated
    // if (!store.state.auth.loggedIn) {
    //     console.log('The user is not logged in - redirecting')
    //     // return redirect('/tool/landing')
    // }
}